export type NavLinks = {
  title: string
  url: string
}[];

export type PaginationLink = {
	srLabel?: string
	text?: string
	url: string
}

export type PageMeta = {
  title: string
  description: string
}